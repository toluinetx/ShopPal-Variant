# Example environment file for `admin`. Copy to `.env` and fill in real values.
# The keys below are MOCK placeholders (correctly formatted, non-functional).

VITE_SERVER_URL=http://localhost:3000

# Product image uploads via S3 pre-signed URLs — mock example key
VITE_AWS_ACCESS_KEY_ID=AKIAJ2XZ3EXAMPLEKEYQ
